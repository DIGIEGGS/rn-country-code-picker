import { StyleSheet } from 'react-native';
import { colors } from '../../theme';

const styles = StyleSheet.create({
  line: { height: 1, width: '100%', backgroundColor: colors.greyLight },
});

export default styles;
